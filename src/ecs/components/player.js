/**
 * Tag component marking the player entity. Also holds the player's score,
 * which increases on box pickups and is used to scale enemy spawn rate.
 */
class Player {
    constructor() {
        this.score = 0;
    }
}